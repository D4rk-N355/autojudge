# 使用官方 Python 基底映像
FROM python:3.11-slim

# 設定工作目錄
WORKDIR /app

# 複製需求檔案並安裝依賴
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 複製程式碼
COPY . .

# 暴露 port（Zeabur 會自動偵測這個 port）
EXPOSE 3000

# 啟動 Flask
CMD ["python", "judge.py"]